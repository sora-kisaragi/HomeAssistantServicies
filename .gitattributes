# Normalize line endings to LF in the repository
* text=auto eol=lf

# Shell scripts must be LF (CRLF breaks shebang on Linux)
*.sh text eol=lf

# Windows-specific files
*.bat text eol=crlf
*.cmd text eol=crlf
