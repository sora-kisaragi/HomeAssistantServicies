extends: default
rules:
  line-length:
    max: 120
    level: warning
  truthy:
    allowed-values: ["true", "false"]
    check-keys: false  # GitHub Actions の "on:" キーを許可
  document-start: disable
  comments:
    min-spaces-from-content: 1
