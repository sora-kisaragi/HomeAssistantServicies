# Root-level shared environment variables
# Copy this file to .env and fill in real values (NEVER commit .env)

# Timezone for all services
TZ=Asia/Tokyo

# Your server's public/local domain or IP
DOMAIN_NAME=localhost

# User/Group IDs for file permission consistency (run: id)
PUID=1000
PGID=1000
