# SearXNG rate limiter configuration
# See: https://docs.searxng.org/admin/searxng-filter-plugins.html

[real_ip]
# X-Forwarded-For header from trusted proxies
x_for = 1
ipv4_prefix = 32
ipv6_prefix = 48

[botdetection.ip_limit]
# Max requests per IP per minute
link_token = true
