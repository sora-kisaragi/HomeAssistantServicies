# PlaywrightMCP environment variables
# Copy to .env and fill in real values (NEVER commit .env)

# MCP server port (also set in docker-compose.yml ports)
MCP_PORT=3000

# Optional: restrict to specific origins (comma-separated)
# MCP_ALLOWED_ORIGINS=http://localhost:3001,http://192.168.1.100:3001
