# Watcher UI environment variables
# Copy to .env and adjust as needed (NEVER commit .env)

# Host port to expose the dashboard
WATCHER_UI_PORT=8767

# URL of the discovery-api (internal Docker network address)
DISCOVERY_API_URL=http://discovery-api:8765

# SSE polling interval in seconds
POLL_INTERVAL=5
